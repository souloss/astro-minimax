---
title: Markdown 功能测试
pubDatetime: 2025-01-01T00:00:00.000Z
author: Souloss
description: 这篇文章用于测试所有 Markdown 特性和自定义组件，包括代码高亮、表格、引用、列表、数学公式、嵌入组件等。
tags:
  - 测试
  - Markdown
  - 教程
featured: true
draft: false
---

import MusicPlayer from "@/components/MusicPlayer.astro";
import Mermaid from "@/components/Mermaid.astro";
import Bilibili from "@/components/Bilibili.astro";
import Alert from "@/components/Alert.astro";

这是一篇全面的 Markdown 功能测试文章，用于验证博客主题对所有 Markdown 特性和自定义组件的支持情况。

---

## 一、基础 Markdown 语法

本节展示标准 Markdown 语法，这些是内置功能，无需额外配置。

### 1.1 段落与换行

这是一个普通段落。Markdown 支持硬换行，只需在行末添加两个或更多空格，然后按回车。

这是新的一行（上面有两个空格）。

这是另一个段落，段落之间用空行分隔。

### 1.2 标题层级

Markdown 支持六级标题：

# 一级标题

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

### 1.3 文字样式

**粗体文字** 使用双星号或双下划线

_斜体文字_ 使用单星号或单下划线

**_粗斜体文字_** 使用三星号

~~删除线文字~~ 使用双波浪线

`行内代码` 使用反引号

普通文字中嵌入 **粗体**、_斜体_、`代码` 和 ~~删除线~~ 的组合效果。

### 1.4 链接

[普通链接](https://example.com)

[带标题的链接](https://example.com "链接标题")

[https://example.com](https://example.com) 自动链接

[email@example.com](mailto:email@example.com) 邮箱链接

[相对路径链接](/posts)

### 1.5 图片

![示例图片](https://picsum.photos/seed/markdown-test/800/400 "图片标题")

---

## 二、引用

### 2.1 基本引用

> 这是一段引用文字。引用可以包含多行文本。
>
> 这是引用的第二段。

### 2.2 嵌套引用

> 第一层引用
>
> > 第二层嵌套引用
> >
> > > 第三层嵌套引用

### 2.3 引用中的其他元素

> #### 引用中的标题
>
> - 引用中的列表项 1
> - 引用中的列表项 2
>
> 引用中的 `行内代码` 和 **粗体** 文字。

---

## 三、列表

### 3.1 无序列表

使用星号、加号或减号：

- 项目 1
- 项目 2
  - 嵌套项目 2.1
  - 嵌套项目 2.2
    - 更深层嵌套 2.2.1
- 项目 3

* 或者使用减号
* 效果相同
  - 嵌套项目

### 3.2 有序列表

1. 第一项
2. 第二项
   1. 嵌套项目 2.1
   2. 嵌套项目 2.2
3. 第三项

### 3.3 任务列表（GFM）

- [x] 已完成的任务
- [ ] 未完成的任务
- [ ] 另一个未完成任务
  - [x] 嵌套已完成
  - [ ] 嵌套未完成

### 3.4 定义列表

术语 1
: 定义 1

术语 2
: 定义 2a
: 定义 2b

---

## 四、代码块

### 4.1 普通代码块

```
这是一个普通代码块
没有语法高亮
可以包含任何文本
```

### 4.2 JavaScript

```javascript
// JavaScript 代码示例
function greet(name) {
  console.log(`Hello, ${name}!`);
}

const arr = [1, 2, 3, 4, 5];
const doubled = arr.map(x => x * 2);

class Person {
  constructor(name) {
    this.name = name;
  }

  sayHello() {
    return `I'm ${this.name}`;
  }
}
```

### 4.3 TypeScript

```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

async function fetchUser(id: number): Promise<User> {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
}

const user: User = {
  id: 1,
  name: "John Doe",
  email: "john@example.com",
};
```

### 4.4 Python

```python
# Python 代码示例
def fibonacci(n: int) -> list[int]:
    """生成斐波那契数列"""
    if n <= 0:
        return []
    elif n == 1:
        return [0]

    fib = [0, 1]
    for i in range(2, n):
        fib.append(fib[i-1] + fib[i-2])
    return fib

# 使用装饰器
@contextmanager
def timer():
    start = time.perf_counter()
    yield
    end = time.perf_counter()
    print(f"耗时: {end - start:.2f}秒")
```

### 4.5 Rust

```rust
// Rust 代码示例
fn main() {
    let numbers = vec![1, 2, 3, 4, 5];

    let sum: i32 = numbers.iter().sum();
    println!("Sum: {}", sum);

    let closure = |x: i32| x * 2;
    let doubled: Vec<i32> = numbers.iter().map(closure).collect();
}

struct User {
    name: String,
    age: u32,
}

impl User {
    fn new(name: &str, age: u32) -> Self {
        User {
            name: name.to_string(),
            age,
        }
    }
}
```

### 4.6 Go

```go
// Go 代码示例
package main

import (
    "fmt"
    "net/http"
)

type Server struct {
    Port int
}

func (s *Server) Start() error {
    return http.ListenAndServe(fmt.Sprintf(":%d", s.Port), nil)
}

func main() {
    numbers := []int{1, 2, 3, 4, 5}
    for i, num := range numbers {
        fmt.Printf("Index: %d, Value: %d\n", i, num)
    }
}
```

### 4.7 CSS

```css
/* CSS 样式示例 */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 1rem;
}

.card {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 1rem;
  padding: 2rem;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-5px);
}
```

### 4.8 HTML

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>示例页面</title>
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><a href="/">首页</a></li>
          <li><a href="/about">关于</a></li>
        </ul>
      </nav>
    </header>
    <main>
      <article>
        <h1>文章标题</h1>
        <p>文章内容...</p>
      </article>
    </main>
  </body>
</html>
```

### 4.9 Shell/Bash

```bash
#!/bin/bash

# 变量定义
NAME="World"
GREETING="Hello"

# 函数定义
say_hello() {
    local name=$1
    echo "$GREETING, $name!"
}

# 循环
for i in {1..5}; do
    echo "Iteration $i"
done

# 条件判断
if [ -f "package.json" ]; then
    echo "Found package.json"
    npm install
fi
```

### 4.10 JSON

```json
{
  "name": "my-project",
  "version": "1.0.0",
  "description": "A sample project",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@types/node": "^20.10.0"
  }
}
```

### 4.11 YAML

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm run build
```

### 4.12 SQL

```sql
-- 创建用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 查询活跃用户
SELECT
    u.username,
    u.email,
    COUNT(p.id) as post_count
FROM users u
LEFT JOIN posts p ON u.id = p.user_id
WHERE u.created_at > '2024-01-01'
GROUP BY u.id
HAVING COUNT(p.id) > 0
ORDER BY post_count DESC
LIMIT 10;
```

### 4.13 代码块特殊功能

#### 行高亮

```javascript {2-3,5}
function highlightExample() {
  // 这一行会被高亮
  const highlighted = true;
  console.log("Hello");
  // 这一行也会被高亮
  return highlighted;
}
```

#### 差异对比

```diff
function greeting(name) {
-  console.log("Hello " + name);
+  console.log(`Hello, ${name}!`);
+  return `Welcome, ${name}`;
}
```

---

## 五、表格

### 5.1 基本表格

| 姓名 | 年龄 | 城市 |
| ---- | ---- | ---- |
| 张三 | 25   | 北京 |
| 李四 | 30   | 上海 |
| 王五 | 28   | 广州 |

### 5.2 对齐表格

| 左对齐 | 居中对齐 | 右对齐 |
| :----- | :------: | -----: |
| 内容 1 |  内容 2  | 内容 3 |
| 数据 A |  数据 B  | 数据 C |

---

## 六、分隔线与转义

### 6.1 分隔线

使用三个或更多的 `-`、`*` 或 `_`：

---

这是第一段内容。

---

这是第二段内容。

---

### 6.2 转义字符

使用反斜杠转义特殊字符：

\* 这不是列表项
\# 这不是标题
\[这不是链接\]
\\ 反斜杠本身

---

## 七、GFM 扩展语法

### 7.1 删除线

~~这段文字被删除了~~

### 7.2 任务列表

- [x] 学习 Markdown 基础
- [x] 掌握 GFM 扩展
- [ ] 编写测试文档
- [ ] 部署到生产环境

### 7.3 自动链接

访问 https://example.com 或发送邮件到 test@example.com

---

## 八、HTML 标签

### 8.1 details 折叠

<details>
<summary>点击展开详情</summary>

这是隐藏的内容，点击展开后显示。

- 可以包含列表
- 也可以包含其他 Markdown 语法
- 甚至可以包含代码块

```javascript
console.log("Hello from hidden content!");
```

</details>

### 8.2 键盘按键

使用 <kbd>Ctrl</kbd> + <kbd>C</kbd> 复制，<kbd>Ctrl</kbd> + <kbd>V</kbd> 粘贴。

快捷键：<kbd>⌘</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd>

### 8.3 下标和上标

H<sub>2</sub>O 是水的化学式。

E = mc<sup>2</sup> 是爱因斯坦的质能方程。

---

## 九、数学公式

> 注意：数学公式功能需要安装 `remark-math` 和 `rehype-katex` 插件。

### 9.1 行内公式

行内数学公式：$E = mc^2$

### 9.2 块级公式

$$
\frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

### 9.3 复杂公式

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

---

## 十、图表支持（Mermaid 组件）

Mermaid 是一个自定义组件，用于渲染流程图、时序图等图表。以下展示其使用方法。

### 10.1 Mermaid 组件参数

| 参数   | 类型   | 必填 | 说明                                     |
| ------ | ------ | ---- | ---------------------------------------- |
| `code` | string | 是   | Mermaid 图表代码（使用反引号模板字符串） |

### 10.2 使用示例

```jsx
<Mermaid
  code={`graph TD
    A[开始] --> B{条件判断}
    B -->|是| C[执行操作1]
    B -->|否| D[执行操作2]
    C --> E[结束]
    D --> E`}
/>
```

### 10.3 流程图效果

<Mermaid
  code={`graph TD
    A[开始] --> B{条件判断}
    B -->|是| C[执行操作1]
    B -->|否| D[执行操作2]
    C --> E[结束]
    D --> E`}
/>

### 10.4 时序图效果

```jsx
<Mermaid
  code={`sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    用户->>前端: 发起请求
    前端->>后端: API调用
    后端->>数据库: 查询数据
    数据库-->>后端: 返回结果
    后端-->>前端: 响应数据
    前端-->>用户: 展示结果`}
/>
```

<Mermaid
  code={`sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    用户->>前端: 发起请求
    前端->>后端: API调用
    后端->>数据库: 查询数据
    数据库-->>后端: 返回结果
    后端-->>前端: 响应数据
    前端-->>用户: 展示结果`}
/>

---

## 十一、自定义组件

本节展示博客支持的自定义组件及其使用方法。这些组件提供了丰富的扩展功能。

### 11.1 Bilibili 视频嵌入

#### 参数说明

| 参数          | 类型   | 必填 | 默认值 | 说明       |
| ------------- | ------ | ---- | ------ | ---------- |
| `bvid`        | string | 是   | -      | 视频 BV 号 |
| `page`        | number | 否   | 1      | 分 P 编号  |
| `width`       | string | 否   | 100%   | 宽度       |
| `height`      | string | 否   | -      | 高度       |
| `aspectRatio` | string | 否   | 16/9   | 宽高比     |

#### 使用示例

```jsx
<Bilibili bvid="BV1rr4y147Lt" />
```

#### 效果展示

<Bilibili bvid="BV1rr4y147Lt" />

### 11.2 音乐播放器

使用 APlayer + MetingJS 支持网易云、QQ音乐等平台。

#### 参数说明

| 参数       | 类型    | 必填 | 默认值  | 说明                        |
| ---------- | ------- | ---- | ------- | --------------------------- |
| `id`       | string  | 是   | -       | 平台歌单/歌曲 ID            |
| `server`   | string  | 否   | netease | 平台：netease/tencent/kugou |
| `type`     | string  | 否   | song    | 类型：song/playlist/album   |
| `autoplay` | boolean | 否   | false   | 自动播放                    |

#### 使用示例

**单曲播放：**

```jsx
<MusicPlayer id="446247397" type="song" server="netease" />
```

**歌单播放：**

```jsx
<MusicPlayer id="391409459" type="playlist" server="netease" />
```

**专辑播放：**

```jsx
<MusicPlayer id="3420143" type="album" server="netease" />
```

#### 效果展示

**单曲：**

<MusicPlayer id="446247397" type="song" server="netease" />

**歌单：**

<MusicPlayer id="391409459" type="playlist" server="netease" />

**专辑：**

<MusicPlayer id="3420143" type="album" server="netease" />

> 注意：需要 VIP 的歌曲只能播放试听部分

### 11.3 Alert 提示块

用于在文章中添加醒目的提示信息。

#### 参数说明

| 参数   | 类型   | 必填 | 默认值 | 说明                                         |
| ------ | ------ | ---- | ------ | -------------------------------------------- |
| `type` | string | 是   | -      | 提示类型：note/tip/important/warning/caution |

#### 使用示例

```jsx
<Alert type="note">
  这是提示信息，用于强调用户应该知道的重要细节。
</Alert>

<Alert type="tip">
  这是建议信息，帮助用户更好地完成任务。
</Alert>

<Alert type="important">
  这是重要信息，用户必须知道的关键内容。
</Alert>

<Alert type="warning">
  这是警告信息，需要用户立即注意以避免问题。
</Alert>

<Alert type="caution">
  这是谨慎信息，提醒用户某些操作可能带来的风险。
</Alert>
```

#### 效果展示

<Alert type="note">这是提示信息，用于强调用户应该知道的重要细节。</Alert>

<Alert type="tip">这是建议信息，帮助用户更好地完成任务。</Alert>

<Alert type="important">这是重要信息，用户必须知道的关键内容。</Alert>

<Alert type="warning">这是警告信息，需要用户立即注意以避免问题。</Alert>

<Alert type="caution">这是谨慎信息，提醒用户某些操作可能带来的风险。</Alert>

---

## 十二、扩展功能

### 12.1 Emoji 表情

支持使用 `:emoji_code:` 语法插入表情：

- :smile: 微笑 `:smile:`
- :heart: 爱心 `:heart:`
- :rocket: 火箭 `:rocket:`
- :fire: 火焰 `:fire:`
- :sparkles: 闪光 `:sparkles:`
- :memo: 备忘 `:memo:`
- :checkered_flag: 终点旗 `:checkered_flag:`

组合使用：:rocket: :fire: :sparkles:

> 完整 Emoji 列表请参考：https://github.com/ikatyang/emoji-cheat-sheet

### 12.2 文字样式扩展

#### 下划线

使用 `<ins>` 标签添加 <ins>下划线文字</ins>。

```html
使用 <ins> 标签添加 <ins>下划线文字</ins>。</ins>
```

#### 高亮标记

使用 `<mark>` 标签添加 <mark>高亮标记文字</mark>。

```html
使用 <mark> 标签添加 <mark>高亮标记文字</mark>。</mark>
```

### 12.3 自定义锚点

使用 HTML `<a>` 标签创建自定义锚点：

<a name="custom-section"></a>

这是自定义锚点位置的内容。

可以通过 `[链接文字](#custom-section)` 跳转到此处。

```html
<a name="custom-section"></a>

这是自定义锚点位置的内容。 可以通过 [链接文字](#custom-section) 跳转到此处。
```

### 12.4 隐藏内容

使用 HTML 注释隐藏内容，这些内容不会在渲染时显示：

{/* 这是一段隐藏的注释，读者看不到 */}

上面有一段隐藏的注释，但在渲染结果中你看不到它。

```jsx
{
  /* 这是一段隐藏的注释，读者看不到 */
}
```

### 12.5 图片扩展语法

#### 图片链接

将图片包裹在链接中，点击图片可跳转：

[![示例图片](https://picsum.photos/seed/markdown-test/400/200)](https://example.com)

```markdown
[![示例图片](https://picsum.photos/seed/markdown-test/400/200)](https://example.com)
```

#### 指定图片尺寸

使用 HTML `<img />` 标签指定图片尺寸：

<img
  src="https://picsum.photos/seed/markdown-test/800/400"
  alt="指定尺寸的图片"
  width="400"
  height="200"
/>

```html
<img
  src="https://picsum.photos/seed/markdown-test/800/400"
  alt="指定尺寸的图片"
  width="400"
  height="200"
/>
```

### 12.6 表格扩展

#### 表格内格式化

表格内支持多种格式：

| 功能                        | 示例          | 说明     |
| --------------------------- | ------------- | -------- |
| **粗体**                    | `**文字**`    | 加粗文字 |
| _斜体_                      | `*文字*`      | 倾斜文字 |
| `代码`                      | `` `代码` ``  | 行内代码 |
| [链接](https://example.com) | `[文字](url)` | 超链接   |

#### 表格内转义

在表格中使用管道符 `|` 需要转义：

| 符号   | 转义写法 |
| ------ | -------- |
| \|     | `\|`     |
| 反引号 | `` \` `` |

### 12.7 引用链接

使用引用式链接保持正文整洁：

这是一个 [引用式链接][ref1]，还有 [另一个链接][ref2]。

[ref1]: https://example.com "示例网站"
[ref2]: https://github.com "GitHub"

```markdown
这是一个 [引用式链接][ref1]，还有 [另一个链接][ref2]。

[ref1]: https://example.com "示例网站"
[ref2]: https://github.com "GitHub"
```

### 12.8 段落与换行详解

#### 段落

段落之间用空行分隔：

这是第一段。

这是第二段。

#### 换行方式

1. **双空格换行**：在行末添加两个空格
   第一行  
   第二行（上面有两个空格）

2. **反斜杠换行**：在行末添加反斜杠
   第一行\
   第二行

3. **HTML 换行标签**：使用 `<br />` 标签
   第一行<br />
   第二行

---