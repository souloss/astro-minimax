---
/**
 * Empty component — renders nothing.
 * Used as fallback when features (Mermaid, Markmap, AI Widget) are disabled.
 *
 * This is a proper Astro component that returns null through the frontmatter,
 * avoiding the need for isAstroComponentFactory internal API hack.
 */
---